# Copy this file to .env and fill in your values.
# Firebase Web API key (from Firebase Console → Project settings → Your apps → Web app → apiKey). Must start with AIza
VITE_FIREBASE_API_KEY=

# Optional: Google Maps (for Create Ride location/geocoding)
# VITE_GOOGLE_MAPS_API_KEY=
